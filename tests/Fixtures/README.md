# Test Fixtures

This directory contains reusable fixture files used by the PHPUnit test suite.

These files are intentionally simple and should only be modified when testing
framework functionality.

Fixtures are shared between:

- ViewFinder tests
- ViewFactory tests
- Page tests
- Future component rendering tests
