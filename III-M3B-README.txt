Phase III.M.3B: Apply to the deployed III.M.3A plugin. Read-only portrait projection using the existing PortraitRenderer. Generated SVG is displayed as an image data URI (no executable inline SVG); custom images use the existing attachment URL. Owner-scoped character listing remains the access boundary. Test on a non-production environment first. Run php vendor/bin/phpunit --display-warnings. No new PHPUnit tests are included; manual checks must include generated/custom/missing portraits and cross-account access.
